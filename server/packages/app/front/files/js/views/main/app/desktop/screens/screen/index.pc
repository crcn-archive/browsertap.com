<div class="screen" data-bind="{{
  style: {
    top: top || 0,
    left: left || 0,
    width: maximize ? '100%' : screen.width,
    height: maximize ? '100%' : screen.height
  }
}}">
  {{ html: `sections.wkm` }}
  <!--div class="screen-border">
  </div-->

  {{ html: `sections.border` }}
  {{ html: `sections.header` }}
</div>
